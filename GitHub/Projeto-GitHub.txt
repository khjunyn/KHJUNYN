
1️⃣ Instalar ferramentas

Android: Termux

PC: Git instalado (git-scm.com)



---

2️⃣ Criar a chave SSH

ssh-keygen -t ed25519 -C "khjunyn@gmail.com" -f ~/.ssh/id_ed25519
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub

ssh-keygen → gera a chave privada e pública.

ssh-add → adiciona a chave ao agente SSH.

cat → mostra a chave pública para adicionar no GitHub.


No GitHub:

Vá em Settings → SSH and GPG keys → New SSH key

Cole a chave pública e salve.



---

3️⃣ Inicializar o repositório Git local

cd /caminho/para/sua/pasta/projeto
git init
git branch -M main
git remote add origin git@github.com:khjunyn/KHJUNYN.git

Cria o repositório local.

Define a branch principal como main.

Conecta o repositório local ao GitHub via SSH.



---

4️⃣ Adicionar e commitar arquivos

git add .
git commit -m "Adicionar arquivos do projeto"

git add . → adiciona todos os arquivos da pasta ao repositório.

git commit -m "..." → cria um commit com uma mensagem descritiva.



---

5️⃣ Enviar arquivos para o GitHub

git push -u origin main

Envia todos os arquivos para o GitHub.

-u → define a branch main para rastrear o repositório remoto.



---

6️⃣ Atualizações futuras

Para qualquer alteração no projeto, só precisa:

git add .
git commit -m "Descrição da alteração"
git push

Não é necessário gerar a chave SSH novamente, a menos que troque de dispositivo.



---

7️⃣ Testar autenticação SSH

ssh -T git@github.com

Deve aparecer:


Hi khjunyn! You've successfully authenticated, but GitHub does not provide shell access.

Confirma que o GitHub reconhece seu dispositivo.



---

💡 Resumo:

Cada dispositivo novo precisa da sua própria chave SSH.

A chave privada nunca vai para o GitHub, só fica no dispositivo.

Depois de configurada, você só usa os comandos de add → commit → push para atualizar o repositório.



---

Se você quiser, KHJUNYN, eu posso agora gerar um PDF desse guia completo, já formatado bonitinho, pronto para você baixar e usar em qualquer dispositivo sem erro.

Quer que eu faça isso agora?

